# HW06 Agent Skill — AI-Driven API Test Generator

P12 artifact implementing the Phase 7 generator concept from `plan.md`.

Artifacts:

- `pseudocode.md` — full internal flow and Human Review Gate.
- `api-test-generator/SKILL.md` — reusable skill contract.
- `api-test-generator/generate.mjs` — deterministic CLI implementation.
- `api-test-generator/README.md` — usage and limitations.
- `demo/` — real generator outputs created from the provided API specification.

The **final self-drawn architecture diagram is intentionally not generated by AI** here because the assignment explicitly requires the submitted diagram to be student-drawn. That artifact belongs to the next diagram step.
