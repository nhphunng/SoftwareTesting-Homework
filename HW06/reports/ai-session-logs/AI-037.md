# AI-037 — FR-16 Step A Requirement Extraction

## User Prompt

@Liebe Dùng $api-testing-human-loop bắt đầu FR16 — Step A: Requirement Extraction cho POST /api/admin/import-products, theo đúng workflow trong plan.md.

## AI Output

Applied the HW06 `api-testing-human-loop` workflow to FR-16 Step A only.

Created `PoolC-FR-16-ImportProducts/analysis/requirements.md` and stopped at Human Gate A.

Key extracted points:

- Selected API: `POST /api/admin/import-products`, Pool C, FR-16.
- FR-16 business contract requires CSV upload, `.csv` extension, exact header `name,price,description,imageUrl,category_id`, RFC4180 quoted-comma support, non-empty `name`, positive `price`, atomic rollback when any row is invalid, and a clear import report.
- The supplied API specification documents the same endpoint as JSON `{ "products": [...] }`, creating an explicit CSV-vs-JSON contract conflict.
- The conflict was preserved as `UNRESOLVED` and not silently normalized.
- Recommended Gate-A model preserves API-contract testing of the JSON endpoint separately from business-compliance testing of FR-16 CSV behavior wherever the real workflow exposes it.
- FR-12/SEC-02/SEC-03 make valid JWT plus `role = 'admin'` directly applicable.
- Exact success/error status codes and exact response schema remain `UNRESOLVED`.
- FR-16 atomicity is a direct persistence invariant: any invalid row means no rows from that batch should persist.
- Whether FR-16 inherits all FR-15 product constraints such as `name <= 255` and existing category validity remains `UNRESOLVED` pending human review.
- Static `server.js` observations were recorded only as implementation observations and runtime targets, not confirmed defects.
- No runtime requests, Newman evidence, screenshots, or bug claims were produced.

Human Gate A remains pending.